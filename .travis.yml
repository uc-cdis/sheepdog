language: python

python: "2.7"

sudo: false

install:
  - pip install -r requirements.txt
  - pip install -r dev-requirements.txt

script:
- py.test -vv --cov=sheepdog --cov-report xml tests/submission/test_load_dictionary.py

after_script:
- python-codacy-coverage -r coverage.xml
